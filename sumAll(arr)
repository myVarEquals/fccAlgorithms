
function sumAll(arr) {
  
  var min = Math.min.apply(null, arr);
  var max = Math.max.apply(null, arr);
  var myNums = 0;
  
  for (i = min; i <= max; i++) {
    myNums += i;
  }
  
  return myNums;  
  
}

sumAll([1, 4]);
